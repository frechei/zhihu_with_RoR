<%= form_for(current_user.follow_questions.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :followed_qs_id, question.id %></div>
  <%= f.submit "关注问题", class: "btn btn-primary" %>
<% end %>